# Cloudflare Workers static assets configuration.
# Build first, then deploy the generated client bundle with `wrangler deploy`.

name = "astro-rocket"
compatibility_date = "2026-04-20"

[assets]
directory = "./dist/client"
