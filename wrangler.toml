# Cloudflare Pages static assets configuration.
# Build first, then deploy the generated client bundle with `wrangler pages deploy`.

name = "astro-rocket"
compatibility_date = "2026-04-20"
pages_build_output_dir = "dist"
